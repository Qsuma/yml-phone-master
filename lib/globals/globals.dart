/* GLobals   determina las funciones de metodo publico para todas las pantallas y almacena los contectoy y estados */
import 'package:flutter/foundation.dart';


    void PPrint(object)  async {
      if (kDebugMode) {
        print(object);
      }
    }